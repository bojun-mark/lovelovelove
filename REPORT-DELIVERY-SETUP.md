# 報告保存與找回更新

## 本次功能

- 已付款報告先寫入 MySQL，保存成功才回傳給網頁。重新查詢讀取同一份報告，不重新呼叫模型。
- 首頁新增「我的訂單與報告」。付款時的瀏覽器可查自己的訂單；其他裝置可輸入私人取回碼。
- 付款前提供私人取回碼及下載備份，確認已保存後才前往付款頁。資料庫只保存取回碼的雜湊值。
- 查詢有效期限為訂單建立後 90 天。這是查詢期限，不代表會自動刪除資料；目前沒有自動清除工作。
- 已付款但尚無報告時可重試，不建立新的付款。多分頁由資料庫鎖定避免同時生成；中斷後最長等待 5 分鐘可重試。
- 可下載完整文字報告。寄信失敗不影響已保存的報告。
- 沒有背景自動重試工作；中斷後需回網站查詢並按重試。模型重試可能產生服務端 API 費用，但不再次向客人發起付款。
- 若原瀏覽器資料與私人取回碼都遺失，不能僅憑 Email 自助找回，以免他人猜信箱取得私人報告。

## 上傳方式

解壓縮後，將裡面的 `client`、`server`、`drizzle` 三個資料夾，以及本說明檔，一起拖入 GitHub 專案根目錄的上傳頁。

不要把外層 `starlovelab_report_delivery_update` 資料夾整個上傳。

提交後等待 Render 最新部署成功，再開網站確認「我的訂單與報告」入口。此壓縮檔本身不會修改線上網站。

## 資料庫

程式啟動時以 `CREATE TABLE IF NOT EXISTS` 新增 `report_deliveries`，不刪除或覆蓋舊訂單。現有 `DATABASE_URL`、`OPENAI_API_KEY` 與 `ca.pem` 請保留。

需要資料庫帳號具有建立資料表的權限。請在正式更新前保留可用的資料庫備份。Aiven 必須運行中；本次功能不會解決免費主機休眠或資料庫關機。

過去未保存的報告無法憑空還原；有效的已付款舊訂單可透過本次流程重新生成，內容可能和當時不同。

## 選用：自動寄信

已接上 Resend 的寄信介面，但預設不啟用。網站會明確顯示「目前未啟用自動寄信」。網站內查閱及下載不依賴寄信服務。

啟用前需自行完成寄信服務與寄件網域設定，並在 Render 設定：

- `RESEND_API_KEY`：Resend API 金鑰，只能放在伺服器環境設定。
- `REPORT_FROM_EMAIL`：Resend 允許的寄件地址，例如你自己已驗證網域的寄件信箱。

沒有提供或建立任何真實寄件帳號、網域或金鑰。不要把金鑰寫進 GitHub 或貼到聊天。

寄件設定完整時，報告保存後會嘗試寄往該訂單原本的 Email；失敗可從報告頁重試。「已交給寄信服務」只代表服務接受請求，不保證收件匣已收到。需另做實際收信測試，也請檢查垃圾郵件。

參考：[Resend 寄信 API](https://resend.com/docs/api-reference/emails/send-email)。

## 驗證範圍與待辦

2026-09-24 本機檢查：

- TypeScript 型別檢查通過。
- 前端正式編譯與後端打包通過；前端有既有的大型檔案提示，不是編譯失敗。
- 19 項自動測試通過（11 項報告權限、保存、重試及寄信隔離測試，另有 8 項既有出生地及星圖測試）。
- 本機模擬訂單頁面確認報告找回、重新整理、未完成報告重試，以及重新探索清除舊訂單狀態。

測試使用模擬資料庫與寄信回應。尚未對正式 MySQL 新資料表、真實付款、OpenAI 生成和真實收信做完整串接測試；沒有收款、扣用 OpenAI 額度或寄出郵件。不能將本次本機測試視為正式收費驗收。

現有付款介面仍沿用原專案的 Stripe，沒有加入綠界更新。正式收費前還需核對付款平台設定、台幣金額單位（原程式直接使用方案整數作為 Stripe unit_amount）、付款通知與對帳。這些付款驗收不包含在本次報告更新內。

部署後驗收順序：先確認網站及資料庫啟動，再用付款平台的測試模式檢查付款、報告保存、重新整理及另一裝置使用取回碼。寄信服務設定後另外測試實際收信。正式付款與 API 費用測試須由站主安排。
