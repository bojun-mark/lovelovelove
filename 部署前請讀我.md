# StarLoveLab 部署準備版

此檔案尚未部署，不能直接視為完整可營運版本。原始壓縮檔保持不變。

已修復付款完成後沒有回傳訂單、缺少訂單查詢函式、分享按鈕沒有執行函式的問題。移除了建置設定中的 Manus 專用外掛與偵錯收集功能。加入 Render 免費方案設定與健康檢查。畫面已改為在頁面閱讀、下載報告，避免承諾尚未實作的 Email 寄送。

## 已完成驗證

- 前端正式版本編譯成功。
- 原有 7 個測試通過；其中星圖測試在未設定外部金鑰時直接略過實際計算，這不代表外部服務已通過驗證。
- 尚未完成真實付款、AI、地圖、資料庫及完整後端上線測試。

## 保留原功能所需設定

1. Render 免費 Web Service 帳號及 GitHub 儲存庫。
2. 外部 MySQL 資料庫，例如 Aiven 免費方案；設定 DATABASE_URL，使用服務商要求的 TLS 設定。
3. STRIPE_SECRET_KEY 與 STRIPE_WEBHOOK_SECRET。請先使用測試模式驗證付款，不要直接收取真實款項。Webhook 路徑為 /api/stripe/webhook。
4. BUILT_IN_FORGE_API_URL 與 BUILT_IN_FORGE_API_KEY 是目前 AI 與地圖共用的原平台服務設定。需要確認是否允許在外部主機使用；若無法使用，必須另行改接服務，不能只換主機。
5. 原檔資料庫遷移紀錄引用了不存在的 0000_cute_sleepwalker.sql，不能直接執行原本 db:push 流程。新資料庫需要依目前 drizzle/schema.ts 建立資料表；舊資料庫需要另行檢查，勿直接套用破壞性操作。

render.yaml 只提供主機設定，沒有代為建立外部服務或資料庫。免費主機不等於 AI、付款等外部服務免費。

## 平台限制（2026-09-17 查核）

Render 免費服務閒置 15 分鐘後休眠，再次訪問啟動約需 1 分鐘；每月共享 750 免費執行小時，超額及頻寬受平台政策限制。官方不建議用於正式營運。
https://render.com/docs/free

Aiven 免費 MySQL 為 1 GB 儲存空間，無試用到期限制，但長時間無使用可能停機。
https://aiven.io/docs/products/mysql/concepts/mysql-free-tier

若要求所有功能零成本，需先確認是否接受將付費結帳和 AI 報告改成免費測驗及規則式報告，再製作相應版本。
